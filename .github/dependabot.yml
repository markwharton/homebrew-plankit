version: 2
updates:
  - package-ecosystem: github-actions
    directory: /
    schedule:
      interval: weekly
    # PRs must land on develop first (develop -> main release flow).
    target-branch: develop
